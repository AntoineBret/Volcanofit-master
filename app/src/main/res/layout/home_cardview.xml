<?xml version="1.0" encoding="utf-8"?>

<android.support.v7.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:app="http://schemas.android.com/apk/res-auto"
  xmlns:card_view="http://schemas.android.com/tools"
  android:id="@+id/cv"
  android:layout_width="match_parent"
  android:layout_height="wrap_content"
  android:layout_gravity="center"
  android:layout_marginBottom="15dp"
  android:layout_marginLeft="15dp"
  android:layout_marginRight="15dp"
  android:layout_marginTop="15dp"
  android:divider="@android:color/transparent"
  app:cardCornerRadius="6dp"
  app:contentPadding="5dp"
  card_view:cardElevation="10dp"
  card_view:cardMaxElevation="15dp">

  <LinearLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <RelativeLayout
      android:layout_width="match_parent"
      android:layout_height="match_parent">

      <ImageView
        android:id="@+id/home_thumbnail"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:adjustViewBounds="true"
        android:clickable="true"
        android:fitsSystemWindows="true"
        android:focusable="true"
        android:paddingRight="10dp"
        android:paddingTop="10dp"
        android:scaleType="fitCenter" />

      <TextView
        android:id="@+id/home_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/home_thumbnail"
        android:fontFamily="@font/montserrat"
        android:gravity="center_horizontal"
        android:paddingLeft="@dimen/cardview_title_padding"
        android:paddingTop="@dimen/cardview_title_padding"
        android:textColor="@color/md_grey_700"
        android:textSize="20dp" />

      <TextView
        android:id="@+id/home_subtitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_below="@id/home_title"
        android:fontFamily="@font/montserrat"
        android:gravity="center_horizontal"
        android:paddingLeft="@dimen/cardview_subtitle_padding"
        android:paddingTop="@dimen/cardview_subtitle_padding"
        android:textColor="@color/md_grey_700"
        android:textSize="15dp" />
    </RelativeLayout>

  </LinearLayout>

</android.support.v7.widget.CardView>
